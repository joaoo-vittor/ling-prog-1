#include <stdio.h>



void main () {

	int linha, coluna;
	int auxlin, auxcol;
	int aux;


	scanf("%d %d", &linha, &coluna);
	int matriz[linha][coluna];

	for (auxlin = 0; auxlin < linha; auxlin++) {
		
		for (auxcol = 0; auxcol < coluna; auxcol++) {
			scanf("%d", &aux);
			
		}

	}

}